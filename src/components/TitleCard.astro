---
interface Props {
  title?: string
  subtitle?: string
}
const { title, subtitle } = Astro.props
---
<article class="title-card">
  <h1>{title}</h1>
  <!-- <div class="subtitle masking-tape">{subtitle}</div> -->
</article>